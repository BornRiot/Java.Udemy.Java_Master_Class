/**
 * This the start of the new section in the course and deals with code blocks, expressions, methods and more
 * This is the download of the first source code used in this section
 * */
package code_blocks_methods_statements;
//IntelliJ will highlight java keywords in blue for the user
//Expressions are the building blocks of all java programs
//Epresiions are built with values, variables and operators and methods calls
class keywords_and_expressions_43 {

    public static void main(String[] args) {
        // a mile is equal to 1.609344 kilometers
        double kilometers = (100 * 1.609344); //This entire thing is a valid java statement. The kilometers up to
        //the 1.609344 however is the expression. The parenthesis are also a part of the expression 
        int highScore = 50;

        if(highScore == 50) {
            System.out.println("This is an expression");

        }







































        // In the following code that I will type below, write down what parts of the code
        // are expressions.
        int score = 100;
        if (score > 99) {
            System.out.println("You got the high score");
            score = 0;
        }












    }
}
